name: 🔄 Clonar a Repo Público

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: 📤 Push Directo (URL PÚBLICA)
        run: |
          echo "🚀 Clonando a Main.6.3 (ahora público)..."
          
          # URL PÚBLICA - SIN TOKEN
          git push https://github.com/Odisey25/Main.6.3.git main:main --force
          
          echo "✅ ¡Éxito! Repositorio sincronizado."