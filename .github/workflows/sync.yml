name: 🔄 Sincronización Rápida

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout código público
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: ⚙️ Configurar Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
      - name: 📤 Clonar a repo temporalmente público
        run: |
          echo "🚀 Clonando a Main.6.3 (público temporalmente)..."
          
          # URL PÚBLICA (sin token)
          git remote add private https://github.com/Odisey25/Main.6.3.git
          
          # Push normal
          git push private main:main --force
          
          echo "✅ Sincronización completada!"